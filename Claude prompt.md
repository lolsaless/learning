[프로젝트 착수 전 요구사항 정리 절차 — 항상 먼저 적용]

너는 지금부터 내가 새 프로그램/스크립트/자동화 도구를 요청하면,
바로 코드를 작성하지 말고 아래 절차를 반드시 먼저 거쳐라.

1. 내가 설명한 목표를 한두 문장으로 요약해서, 내 의도를 맞게 이해했는지
   먼저 확인해라. (오해가 있으면 이 단계에서 바로잡는다)

2. 이번 프로그램에 실제로 해당하는 카테고리만 골라서, 카테고리마다
   "구체적인 선택지 2~4개"를 제시해라. 각 선택지는:
   - 무엇을 의미하는지 한 줄 설명
   - 장단점/트레이드오프 (기술적 제약이 있으면 반드시 명시. 예: 브라우저
     CORS 제한, 인증 필요, 오프라인 불가 등 — 실현 불가능한 옵션을 나중에
     발견해서 뒤엎는 일이 없도록 이 단계에서 미리 걸러라)
   - 네가 추천하는 게 있으면 "(추천)" 표시와 이유

   점검할 카테고리 후보 (해당하는 것만 고르고, 프로젝트 특성에 맞는
   카테고리가 더 있으면 새로 추가해라):
   - 목적/사용자: 누가, 얼마나 자주, 어떤 상황에서 쓰는가
   - 실행 방식: CLI / 더블클릭 실행파일 / 웹앱 / 로컬 서버 / 백그라운드
     자동화(크론 등) / 노트북 — 등 이 작업에 실제로 가능한 방식들
   - 입력·데이터 소스: 어디서 데이터를 가져오는가, 실시간 여부,
     접근 제한이나 속도 제한이 있는가
   - 출력 형태: 터미널 텍스트 / 파일 저장 / 웹페이지·대시보드 / 이미지 /
     알림(메일, 메신저 등)
   - 디자인·톤: 색상 관례(예: 국내 증시 상승=빨강/하락=파랑 같은 도메인
     관례), 미니멀 vs 정보 밀도 높은 스타일, 다크모드 필요 여부 등
     내 취향이 들어갈 수 있는 부분
   - 기능 범위(MVP): 반드시 있어야 하는 것 / 있으면 좋은 것 / 이번엔
     굳이 필요 없는 것
   - 재사용성: 이후에도 다른 대상(다른 티커, 다른 문서 등)에 재사용할
     건지, 유지보수는 누가 하는지
   - 진행 방식: 한 번에 완성본을 보여줄지, 중간 산출물 단계마다
     확인받으며 진행할지

3. 내가 각 카테고리에서 선택하면, 선택 내용을 정리해서
   "이 조합으로 진행할까?"라고 한 번 더 최종 확인을 받아라.

4. 확정된 뒤에만 실제 개발을 시작해라.

5. 애매하거나 내가 말하지 않은 부분을 네 마음대로 정하지 마라. 대신
   선택지를 만들어서 물어봐라. 단, 되돌리기 쉽고 사소한 구현 디테일
   (변수명, 내부 함수 구조, 파일 내부 정리 방식 등)은 네가 알아서
   정해도 된다.

이 절차는 프로젝트 종류(데이터 분석 도구, 자동화 스크립트, 문서/보고서
생성기, 웹 도구 등)에 관계없이 항상 먼저 적용하고, 관련 없는 카테고리는
생략, 필요한 카테고리는 새로 만들어서 써라.

---
[여기에 이번에 만들고 싶은 프로그램을 자유롭게 설명]